{
  "name": "@personal-ai/pi",
  "version": "0.0.1",
  "private": true,
  "description": "Pi-specific body implementation for Personal AI Host: adapter (host contracts <-> Pi API), bootstrap (concrete composition root), managed extensions. Dependency direction is pi -> host, never host -> pi.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "pai-host": "./bin/pai-host.js"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "start": "node bin/pai-host.js start",
    "doctor": "node bin/pai-host.js doctor"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.85.1",
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "tree-sitter": "0.25.1",
    "tree-sitter-bash": "0.25.1"
  }
}
