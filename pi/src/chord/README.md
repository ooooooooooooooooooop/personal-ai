# chord/ — M5 placeholder

Chord（`@earendil-works/chord` 0.85.1）组合运行时适配：facets / keyed services / replicated state / remote sources → host contracts。

候选承接 Cordis 分流中的 C 类服务。**未担保等价**——M5 前做一次 spike 对齐语义差异。

纪律：Chord 是 concrete runtime，放在 pi 身体内——即使它本身零 Pi 依赖，也不许进 `host/`。
