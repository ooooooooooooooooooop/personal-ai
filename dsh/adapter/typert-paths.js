/**
 * Wire path constants for the DSH /api surface (mirrors
 * @deepseek-ai/dsh-client-connection's api-path contract — kept local so the
 * adapter does not import the managed install's packages).
 */
export const API_PATH = '/api';
export const MUX_EVENTS_PATH = '/api/events.mux';
export const HOST_EVENTS_PATH = '/api/events.host';
