# 增量采集 2026-09-22 —— 距全量采集（09-20）两日 + 近期四周趋势面

来源：官方 changelog / GitHub releases / 官方博客，当日抓取。仅收信号条目（功能/机制向），模型上架、定价、纯修复不收。

## Claude Code（2.1.277–2.1.278）
- AGENTS.md 一等支持：无 CLAUDE.md 时读 AGENTS.md，`/config` 可改（我方已有 ✅）
- auto mode 服务端 classifier 默认化（不计费开销）；`/status` 增 "Auto mode server" 行
- `CLAUDE_GATEWAY_PROXY_IS_EGRESS_BOUNDARY=1`：forward-proxy 作为唯一 egress 边界，按 hostname 放行
- 提示词净化：不可见 Unicode 格式/标签字符在发送前剥除并展示清洗结果（我方 sanitizeArgs 已覆盖工具参数层 ✅；用户 prompt 入口未做）
- `sandbox.excludedCommands` glob 修复：复合命令必须**每个分段**都命中豁免（我方 governance.js 已同款：每 unit 匹配 ✅）
- dangerous-rm 审批提示建议 `${VAR:?}` 守卫写法（headless 可恢复）
- 后台任务在 `/tasks` 面板打开时完成 → 行内提示

## Codex CLI（0.152–0.155.1，09-01~09-18）
- 0.155: `/voice` 实验性语音；agents-overview 任务管理；**Touch ID 验证 MCP 请求**（macOS）；daemon 更新计划；线程/goal 在 daemon 重启后恢复；**账号切换使旧身份的 remote-control 会话/缓存 WS/模型目录全部失效**
- 0.152: vim `/` `?` 草稿搜索；rate-limit banner 带操作入口；**每 MCP 工具 `output_token_limit`**（resume 一致截断）；app-server `thread/shellCommand` 超时；凭据刷新进度可见
- 任务生命周期：archive/delete/受管 worktree 归属与干净删除确认

## Cursor（08-19~09-10）
- **Projects**（09-10 beta）：coordinator agent 不写码只分解+委派；项目级文件集跨云/本机同步（含 agent 学到的 codebase 知识与偏好）；信号触发（watch Slack 频道/定时/PR 跟随）→ 无需提示即行动
- **Cursor Router**：Auto 模式由路由器驱动，Cost/Balance/Intelligence 三档；管理员可按 team/group 开关、限档、默认档、模型 allow/block、软/硬强制；被路由模型可隐藏
- Self-hosted machines：工具执行全在自有网络；支持 Lambda/Coder/Cloudflare/Daytona/Modal/Namespace/Vercel/E2B；Linux/Mac computer use
- Builds：环境预热副本，上次成功 build 兜底，agent 3x 提速启动
- CLI（08-26）：**持久会话** `agent persist` + `/detach` + `attach`
- Google Workspace 插件（Gmail/Drive/Calendar 读写）

## Gemini CLI（v0.58–0.60，09-01~09-15，安全加固季）
- 扩展请求环境变更 → 显式用户同意 + 运行时改造型 env var 消毒
- 沙箱容器内隔离 settings 目录与临时目录；macOS Seatbelt 隔离 Docker socket
- **NTFS 8.3 短名（SFN）缓解**；symlink 解析一致化；workspace 边界校验强化
- MCP OAuth 强制 RFC 9207 issuer 识别
- **envelope metadata provenance：不可信工具输出强制来源标记**
- 系统级配置路径严格权限/属主检查；删除 chrome-devtools-mcp 硬编码 API key
- 0.54: Antigravity agent runner 集成 + Firestore 双锁并发；HTTPS 强制防明文凭据；模型 fallback 轮换 session ID

## OpenHands（v1.17–1.20，09-09~09-17）
- **agent profile 可限定到指定 MCP server 子集**
- 注入 runtime config 时 HTML 转义 + credential 注入响应 `Cache-Control: no-store`
- automation 停用原因在 UI 明示；图片附件点击全尺寸

## Devin（09 月）
- **Code Scans `/scan`（09-16）**：Agentic MapReduce——plan（定规则）→shard（分批）→map（并行 agent 查批）→reduce（去重+优先级合成报告）→ findings 转 PR
- **Fusion（09-11）**：双模型 harness——frontier lead 管 session/plan/review，廉价 sidekick 执行；两个并行 agent 各有持久 context 和工具；基准上省 ~39% 成本
- 大粘贴（10k+ 字符）自动转文件附件（不受已有消息长度影响）
- Personal Automations + **files-changed 触发器**（push 按路径过滤）；Review 新 commit 自动取消进行中评审；新命令面板 Cmd/Ctrl+K

## OpenCode（v1.18.26–1.18.31）
- 多为 provider/模型兼容；ACP session model/effort/mode/reasoning 边界恢复；console batch workspace block 端点

## Pi-mono（badlogic，0.84.x 至 08-28）
- 无 9 月新 release；本地已收全史

## Windsurf
- 9 月无官方 changelog 条目；近期线：Devin Review/Quick Review 全量下放、Agent Command Center 列表化、JetBrains 插件获 SWE 1.5 + Fast Context
